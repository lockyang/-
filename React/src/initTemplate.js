import React, { Component, PropTypes } from 'react';

class App extends Cpmponent {
  static PropTypes = {
    // 定义传值类型
  }

  static defaultProps = {
    // 默认传值
  }

  consructor(props) {
    this.state = {
      // 定义状态
    }
  }

  componentWillMount () {
    // 渲染前
  }

  componentDidMount () {
    // 渲染后
  }

  render () {
    return (
      // 
    )
  }
}